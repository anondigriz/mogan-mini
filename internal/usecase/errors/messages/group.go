package messages

const (
	CreateGroupInStorageFail   = "fail to create the group in the storage"
	GetGroupFromStorageFail    = "fail to get the group from the storage"
	UpdateGroupInStorageFail   = "fail to update the group base in the storage"
	RemoveGroupFromStorageFail = "fail to remove the group from the storage"
)
