package messages

const (
	CreateRuleInStorageFail   = "fail to create the rule in the storage"
	GetRuleFromStorageFail    = "fail to get the rule from the storage"
	UpdateRuleInStorageFail   = "fail to update the rule base in the storage"
	RemoveRuleFromStorageFail = "fail to remove the rule from the storage"
)
