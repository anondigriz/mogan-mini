package messages

const (
	CreatePatternInStorageFail   = "fail to create the pattern in the storage"
	GetPatternFromStorageFail    = "fail to get the pattern from the storage"
	UpdatePatternInStorageFail   = "fail to update the pattern base in the storage"
	RemovePatternFromStorageFail = "fail to remove the pattern from the storage"
)
